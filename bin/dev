#!/usr/bin/env ts-node

import { dev } from '../dist/dev.js';

const devProjectRoot = process.argv[2]; // This is the tempDir passed from runDevCommand
const cliArgs = process.argv.slice(3); // Actual CLI arguments start from the third position

// Temporarily set process.argv for parseArguments
const originalArgv = process.argv;
process.argv = ['node', 'script.js', ...cliArgs]; // Mock process.argv for parseArguments

dev(devProjectRoot); // Pass the tempDir as projectRoot

process.argv = originalArgv; // Restore original process.argv
